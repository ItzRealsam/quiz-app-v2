import { 
  renderCurrentScreen 
} from "../core/render.js";

import { 
  appState 
} from "../core/state.js";

export function handleProfileAction(
  action,
  target
) {

  switch (action) {

    default:

      return false;

  }

}