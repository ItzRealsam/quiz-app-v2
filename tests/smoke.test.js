import {
  describe,
  expect,
  it
} from 'vitest';

describe(
  'Smoke Test',
  () => {

    it(
      'should work',
      () => {

        expect(
          2 + 2
        ).toBe(4);

      }

    );

  }
);